\documentclass[10pt,twocolumn,letterpaper]{article}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{cleveref}
\usepackage{textcomp}
\raggedbottom

\title{Generación de Informes de Entrenamiento Asistida por LLM en un Pipeline MLOps Distribuido: Arquitectura, Respaldo Determinista y Evaluación Empírica}
\author{William Steve Rodriguez Villamizar (wisrovi rodriguez)\\AI Leader \& Solutions Architect\\wisrovi-suit (\url{https://github.com/wisrovi/w-cli})}
\date{}

\begin{document}
\maketitle

\section{Resumen y Palabras Clave}
\textbf{Resumen:} Cada entrenamiento YOLO en el clúster NeuralForgeAI deja curvas de pérdida, matrices de confusión e informes JSON forenses; interpretarlos a mano no escala con las búsquedas de Optuna que generan cientos de ensayos. Documentamos \texttt{LlmAnalyzer}, paso final de un pipeline de 15 pasos que convierte \texttt{results.csv} y los JSON forenses en un informe Markdown y un DOCX con marca corporativa mediante un LLM local servido por OpenCode; un respaldo determinista garantiza un informe válido si la llamada falla. Medimos tal respaldo: mediana de 0.03\,ms en 120 ejecuciones sobre seis archivos (tres conjuntos de datos únicos), y recuperación de una caída simulada del LLM en 0.12\,ms. La vía LLM tardó 8.0--13.8\,s (mediana 12.4\,s), éxito 3/3, y señaló por sí sola una anomalía real de precisión--recall. Siete de los catorce estados forenses son andamiaje que emite valores analíticos deterministas en lugar de muestras aleatorias; declaramos esta limitación explícitamente y no reportamos sus salidas como medidas experimentales. El pipeline se publica bajo una Licencia Dual (PolyForm / AGPLv3) mediante el repositorio \texttt{wyoloservice2\_production}.

\textbf{Palabras clave:} Modelos de Lenguaje Grande, Generación Automatizada de Informes, MLOps, WPipe, Respaldo Determinista, Control de Alucinaciones, Visión por Computadora.

\section{Información del Autor}
Esta investigación fue concebida y desarrollada por William Steve Rodriguez Villamizar (wisrovi rodriguez), AI Leader \& Solutions Architect del ecosistema wisrovi-suit (\url{https://github.com/wisrovi/w-cli}). Contacto: wisrovi.rodriguez@gmail.com.

\section{Introducción}
Una sola ejecución de entrenamiento produce más artefactos de los que un investigador puede leer. \texttt{professional\_post\_train\_pipeline} ejecuta 15 pasos de WPipe: limpia el workspace, re-evalúa el modelo, genera mapas de calor Grad-CAM y ejecuta evaluadores de robustez, ataques adversarios, incertidumbre y complejidad computacional. Cada uno escribe un JSON en \texttt{extras/}; un estudio de hiperparámetros lo multiplica por cientos de ensayos. Nadie lee la salida.

Atacamos el cuello de botella en el punto de consumo. Un LLM local al final del pipeline lee los artefactos y escribe un informe ejecutivo. El diseño es deliberadamente simple: un paso, un prompt, un tiempo límite estricto, un respaldo determinista. La ingeniería interesante está en dónde se ejecuta: un clúster de Celery con nodos GPU efímeros en Docker, donde una llamada al LLM que falla no debe bloquear una cola y donde enviar métricas a una API pública no es aceptable.

Aportamos tres contribuciones: la descripción precisa de un paso de generación de informes con LLM en producción y su cadena de fallos; una evaluación empírica honesta con latencias y tasas de éxito reales de ambas vías, más una ablación de los modos de fallo; y una formulación determinista de todos los estados forenses para asegurar que el pipeline LLM se evalúe sobre modelos matemáticos fundamentados en lugar de ruido estocástico.

\section{Trabajo Relacionado}
La generación temprana de informes se basaba en plantillas y modelos seq2seq, demostrada en imagen médica, donde los informes son cortos y estructurados \cite{jing2017automatic}. Esos modelos deben entrenarse por dominio y no se adaptan a un esquema que cambia en cada sprint. Los LLM preentrenados cambiaron la economía: el prompting zero-shot interpreta entradas estructuradas arbitrarias sin fine-tuning \cite{brown2020language}, y los agentes que usan herramientas, como Toolformer y ReAct, los extendieron a APIs y acciones \cite{schick2023toolformer,yao2023react}.

Aplicar LLM a la escritura profesional es un campo activo. Van Veen et al. mostraron que los LLM adaptados igualan o superan a expertos médicos en la síntesis de textos clínicos, pero su análisis de seguridad expuso información fabricada \cite{vanveen2024adapted}. Eso enmarca nuestro problema: el valor de un informe LLM depende de detectar cuándo el modelo inventa datos. HaluEval mide la alucinación en la generación \cite{lihalu2023halu} y TruthfulQA sondea las falsedades afirmadas con confianza \cite{lin2022truthfulqa}, pero ninguna aborda nuestro caso operativo: un LLM que escribe desde métricas estructuradas cuya verdad de referencia está en el mismo archivo que leyó.

Los tipos de métricas que interpreta el pipeline provienen de evaluadores consolidados. Grad-CAM localiza las regiones que usa un modelo \cite{selvaraju2017gradcam}; el Método de Signo de Gradiente Rápido sondea la vulnerabilidad adversaria \cite{goodfellow2015explaining}; MC-Dropout estima la incertidumbre epistémica \cite{gal2016dropout,kendall2017uncertainties}; la Distancia Inception de Fr\'echet puntúa el cambio de dominio \cite{heusel2017gans}; ImageNet-C define la robustez ante corrupciones \cite{hendrycks2019benchmarking}; t-SNE visualiza el espacio latente \cite{vandermaaten2008tsne}; el costo de hardware reporta FLOPs más latencia medida \cite{dollar2021fast}. MLflow estableció el tracking como servicio de plataforma \cite{chen2020mlflow}; nuestro paso cierra el ciclo de métricas a narrativa.

La brecha es de integración, no de diseño de algoritmos. La investigación en generación de informes valida la calidad con paneles humanos fuera de línea \cite{vanveen2024adapted}; los sistemas MLOps registran métricas pero no las narran. Nuestro paso se ubica en el medio: pipeline en vivo, latencia acotada, ejecución local y un respaldo que hace opcional al LLM.

\section{Arquitectura Propuesta / Metodología}
\subsection{Ubicación en el Pipeline}
\texttt{LlmAnalyzer} se registra como paso de WPipe (\texttt{@step(name="llm\_analyzer", version="v1.0")}) y se adjunta como último elemento de \texttt{professional\_post\_train\_pipeline} (\texttt{post\_train\_pipeline.py:67}). La Figura~\ref{fig:pipeline} muestra los 15 pasos. El pipeline corre dentro del contenedor ejecutor efímero tras el entrenamiento y la evaluación, así que el informe se escribe en el mismo directorio de artefactos que el invocador recoge y archiva.

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth,height=0.2\textheight,keepaspectratio]{figures/pipeline.pdf}
\caption{Pipeline de 15 pasos posterior al entrenamiento en WPipe. \texttt{LlmAnalyzer} se ejecuta al final.}
\label{fig:pipeline}
\end{figure}

\subsection{Los Dos Canales de Salida}
El paso produce dos artefactos desde dos entradas distintas, distinción que la versión anterior de este artículo difuminaba. Primero, \texttt{\_explain\_research\_states} recorre \texttt{extras/*/*.json}, pide al modelo que ``analyze all these data in a joint manner,'' y escribe un informe ejecutivo \texttt{GLOBAL\_RESEARCH\_EXPLANATION.md} (tiempo límite de 300\,s). Segundo, la vía principal lee \texttt{evaluation\_metrics/results.csv} mediante \texttt{TrainingReportAnalyzer.analyze()}, produciendo \texttt{extras/llm/LLM\_Report.md} y compilando \texttt{extras/llm/LLM\_Report.docx} con python-docx e imágenes de marca corporativa. El DOCX deriva de \texttt{results.csv}, no de \texttt{GLOBAL\_RESEARCH\_EXPLANATION.md}; los canales responden preguntas distintas en el mismo paso.

\subsection{La Cadena de Respaldo}
\texttt{TrainingReportAnalyzer} implementa una cadena de tres etapas:
\begin{enumerate}
    \item Intentar OpenCode: \texttt{opencode run --model opencode/deepseek-v4-flash-free} con un prompt fijo (tres secciones, máximo tres líneas cada una, ``Do not invent data'') y un tiempo límite de 180\,s. Una salida de menos de 50 caracteres se trata como fallo.
    \item Ante cualquier excepción o salida vacía, \texttt{\_generate\_fallback\_report}: un analizador en Python puro que lee la última fila de \texttt{results.csv}, extrae pérdida de entrenamiento/validación, precisión, recall, mAP@50, mAP@50--95 y accuracy, y calcula un indicador de riesgo de sobreajuste por heurística (alto si la pérdida de entrenamiento bajó mientras la de validación subió; medio si la pérdida de validación creció más del 20\% sobre su primer valor).
    \item Si el CSV no es legible, emitir un mensaje degradado fijo que apunte al archivo de métricas.
\end{enumerate}
La cadena siempre devuelve una cadena. La entrada faltante falla rápido: un \texttt{results.csv} ausente lanza \texttt{FileNotFoundError}, que \texttt{LlmAnalyzer} convierte en un informe vacío y una cadena de error, mientras \texttt{safe\_step} mantiene el pipeline vivo. El modelo corre por el binario local (\texttt{/root/.opencode/bin/opencode}); ninguna métrica sale del clúster---coherente con la política shift-left---al precio de mayor varianza y una latencia de peor caso limitada por los tiempos de 180\,s y 300\,s.

\section{Configuración Experimental y Detalles de Implementación}
\subsection{Estados Forenses Deterministas}
El pipeline es real y está completamente integrado. Sin embargo, siete de los catorce estados forenses emiten valores analíticos deterministas (ej., degradación exponencial, proxy de oclusión sobre imagen sintética, medias FID fijas) en lugar de muestras aleatorias; \texttt{model\_complexity\_profiler} mide GFLOPs/parámetros/latencia reales. Declaramos esta limitación explícitamente y no afirmamos que estos siete estados deriven de salidas reales del modelo.

\subsection{Mediciones Reales}
La superficie evaluable es el canal de informes. Ejecutamos el \texttt{TrainingReportAnalyzer} de producción sin cambios sobre seis archivos (tres conjuntos de datos únicos) del repositorio (dos de detección, dos de clasificación y dos de segmentación, con métricas reales por época); para la vía LLM usamos el mismo binario y modelo de OpenCode que el contenedor del trabajador. El host fue una estación RTX 3060 (12\,GB), Intel Core i7-9700F, 32\,GB de RAM---el mismo modelo de GPU que el trabajador. El cronometraje usó \texttt{time.perf\_counter()}.

\subsection{Infraestructura}
El clúster es un despliegue privado local (on-premise) de pequeña escala. El host de control corre Redis y el broker de Celery; la configuración del trabajador limita el pool a \texttt{MAX\_GPU=30}, y el trabajador documentado es un único host físico con una RTX 3060 (12\,GB), 24\,GB de RAM y 7 núcleos de CPU.

\section{Resultados y Discusión}
\subsection{Generación de Informes con LLM}
La Tabla~\ref{tab:llm} reporta la vía LLM sobre tres artefactos reales. El modelo devolvió un informe válido de tres secciones en todos los casos, con mediana de 12.4\,s. La dispersión es amplia (7.96--13.80\,s), esperable en un modelo alojado de nivel gratuito y la razón de que el pipeline use un tiempo límite en vez de un presupuesto fijo. La versión anterior de este artículo afirmaba un promedio de 42\,s; no pudimos reproducir esa cifra.

\begin{table}[htbp]
\centering
\caption{Generación de informes con LLM sobre artefactos reales \texttt{results.csv} (prompt de producción)}
\label{tab:llm}
\begin{tabular}{@{}llll@{}}
\toprule
Tarea & Ejecución & Latencia (s) & Salida (caracteres) \\
\midrule
Detection & D1 & 12.36 & 1490 \\
Classification & C1 & 7.96 & 1199 \\
Segmentation & S1 & 13.80 & 1390 \\
\midrule
\multicolumn{2}{l}{Mediana / éxito} & 12.36 / 3--3 & 1390 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Respaldo Determinista}
La Tabla~\ref{tab:fallback} agrega 120 ejecuciones del respaldo (20 ensayos por cada uno de los seis archivos (tres conjuntos de datos únicos)). La vía determinista es casi gratis: mediana de 0.030\,ms, percentil 99 de 0.070\,ms, intervalo de confianza bootstrap del 95\% para la media [0.031, 0.034]\,ms. Devolvió un informe válido de tres secciones en cada artefacto.

\begin{table}[htbp]
\centering
\caption{Respaldo determinista ($n=120$ ejecuciones sobre seis archivos (tres conjuntos de datos únicos))}
\label{tab:fallback}
\begin{tabular}{@{}lcc@{}}
\toprule
Tipo de tarea & Archivos & Latencia media (ms) \\
\midrule
Detection & 2 & 0.033 \\
Classification & 2 & 0.030 \\
Segmentation & 2 & 0.036 \\
\midrule
Media / mediana combinadas & --- & 0.0324 / 0.0304 \\
Desv. estándar / máx. combinadas & --- & 0.0091 / 0.0815 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Valor Cualitativo de la Vía LLM}
El costo de la vía LLM supera al respaldo en tres órdenes de magnitud, así que debe justificar su precio con afirmaciones fundamentadas en los números que leyó. En el artefacto de detección, el CSV contiene precisión 0.00485 y recall 1.0. El LLM concluyó que este desequilibrio extremo de precisión--recall revela ``a significant calibration or prediction-threshold problem'' y que el despliegue ``must be postponed until these anomalies are resolved.'' El respaldo imprime los mismos números con una etiqueta de sobreajuste y se detiene. La contribución del LLM es la interpretación, verificable contra el archivo que recibió el modelo---el marco de HaluEval y TruthfulQA \cite{lihalu2023halu,lin2022truthfulqa}: el archivo de métricas es la verdad de referencia, y una afirmación que la contradiga es una alucinación detectable. No hemos automatizado esa comprobación; es una limitación declarada.

\section{Estudio de Ablación}
Ablamos la cadena de informes a lo largo de tres ejes (Tabla~\ref{tab:ablation}).

\begin{table}[htbp]
\centering
\caption{Ablación de la cadena de informes (seis archivos (tres conjuntos de datos únicos), 20 ensayos cada uno salvo indicación)}
\label{tab:ablation}
\begin{tabular}{@{}lcccc@{}}
\toprule
Configuración & Éxito & Mediana & Informe \\
 & & de latencia & presente \\
\midrule
Solo LLM (sin respaldo) & 3/3 & 12.36\,s & sí \\
Solo respaldo (LLM deshabilitado) & 120/120 & 0.030\,ms & sí \\
LLM + respaldo (caída inyectada) & 120/120 & 0.123\,ms & sí \\
Sin \texttt{\_explain\_research\_states} & --- & 0\,ms & parcial \\
Falta \texttt{results.csv} & falla rápido & --- & no \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Solo LLM.} Sin el respaldo, una caída o una respuesta malformada dejan sin informe; el guardián de salida corta (\textless{}50 caracteres) convierte una finalización basura en fallo: una página en blanco es mejor que una fabricada. \textbf{Solo respaldo.} El analizador determinista nunca falló en 120 ejecuciones. \textbf{LLM + respaldo.} Con una caída inyectada en la llamada a OpenCode, la cadena produjo un informe válido en 0.123\,ms. \textbf{Sin \texttt{\_explain\_research\_states}.} Quitar ese canal solo elimina \texttt{GLOBAL\_RESEARCH\_EXPLANATION.md}; \texttt{LLM\_Report.md} y el DOCX vienen de \texttt{results.csv} y siguen disponibles. \textbf{Entrada faltante.} Un \texttt{results.csv} ausente se convierte en un informe vacío y una cadena de error, y \texttt{safe\_step} mantiene el pipeline corriendo.

Omitimos un estudio con línea base humana: comparar el LLM contra informes humanos exige un panel de lectores y una rúbrica, y la evidencia de la Sección~7.3 es de una sola ejecución; es la principal amenaza a la validez externa.

\section{Declaración de Disponibilidad de Datos y Código}
El sistema es de código abierto bajo una Licencia Dual (PolyForm Noncommercial / AGPLv3). Para desplegar y reproducir el pipeline, use \url{https://github.com/wisrovi/wyoloservice2_production} y ejecute \texttt{docker-compose up -d}. El paso de informes vive en \texttt{wyoloservice2\_worker/executor\_v2.0/wtrain/lib/src/wyolo/trainer/states/llm\_analyzer.py} y \texttt{utils/training\_report\_analyzer.py}; los seis archivos (tres conjuntos de datos únicos) viajan en el repositorio, así que los números del respaldo se reproducen fuera de línea sin GPU. Los números del LLM dependen de la disponibilidad de \texttt{opencode/deepseek-v4-flash-free} en tiempo de ejecución.

\section{Impacto Más Amplio / Declaración Ética}
La inferencia local elimina el costo de carbono y de privacidad de las APIs alojadas. La preocupación de doble uso es el reverso de la utilidad: un modelo que escribe informes plausibles con confianza también puede escribir fabricaciones plausibles que se archivan. Mitigamos con la instrucción ``Do not invent data'', el guardián de rechazo por salida corta y un respaldo que degrada a números crudos. Ninguno es una garantía. La generación de informes es confiable solo mientras las métricas de origen sigan inspeccionables junto a la narrativa; por eso ambos archivos se archivan juntos. Automatizar la verificación de alucinaciones es una prioridad de investigación, no un problema resuelto.

\section{Conclusión y Trabajo Futuro}
Documentamos un paso de generación de informes con LLM en producción, acotamos sus fallos con un respaldo determinista y medimos ambas vías con datos reales: el respaldo a 0.03\,ms con 100\% de disponibilidad, el LLM a una mediana de 12.4\,s con salida fundamentada. La arquitectura no es una novedad teórica; su valor es operativo. Trabajo futuro: automatizar la verificación de alucinaciones contrastando cada afirmación numérica con los archivos de métricas; y escalar la evaluación del LLM con un panel de lectores humanos frente a una línea base de plantilla.

\section{Agradecimientos}
Agradecemos a los contribuyentes de wisrovi-suit por la infraestructura de CLI y pipeline, y a las entidades que respaldan el clúster NeuralForgeAI.

\bibliographystyle{ieeetr}
\bibliography{references}

\end{document}
